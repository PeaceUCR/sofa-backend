CREATE TABLE judgment_detail
(
    rowKey          character varying(36) PRIMARY KEY,
    textHead        text,
    textPartner     text,
    textMain        text,
    textDecide      text,
    textJudge       text
);